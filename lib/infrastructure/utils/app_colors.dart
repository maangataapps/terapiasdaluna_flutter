import 'package:flutter/material.dart';

class AppColors {
  static const foodColor = Colors.red;
  static const sportColor = Colors.amber;
  static const sleepColor = Colors.blue;
  static const poopColor = Colors.brown;
  static const calendarColor = Colors.green;
  static const supplementsColor = Colors.pink;
  static const adminColor = Colors.teal;
  static const reminderColor = Colors.teal;
  static const questionsColor = Colors.purple;
}